package events

// Event name constants for the Token aggregate.
// Use these constants when subscribing to or publishing token events.
const (
	NameTokenCreated = "token.created"
	NameTokenRevoked = "token.revoked"
)
